name: clean_marvel
description: A Flutter project to learn clean architecture.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  freezed_annotation: ^2.2.0
  http: ^1.1.0
  internet_connection_checker: ^1.0.0+1
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.0.15

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.2
  flutter_test:
    sdk: flutter
  freezed: ^2.3.5
  isar_generator: ^3.1.0+1
  mockito: ^5.4.2

flutter:
  uses-material-design: true
